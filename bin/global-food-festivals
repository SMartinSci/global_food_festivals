#!/usr/bin/env ruby
#Shebang operator, uses the env command to figure out where the Ruby interpreter is and run it.
#Tells our program what program to run our file with by asking for the current configured version of Ruby

#class CLI to instantiate and use the call method on
#Executable file
require_relative "../lib/global_food_festivals"

CLI.new.call #Runs program
